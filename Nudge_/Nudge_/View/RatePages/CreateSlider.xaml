<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Nudge_.View.CreateSlider"
             xmlns:behaviours="clr-namespace:Nudge_.Behaviours"
             xmlns:Converters="clr-namespace:Nudge_.Converters"
             xmlns:syncfusion="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             xmlns:helpers="clr-namespace:Syncfusion.ListView.XForms.Helpers;assembly=Syncfusion.SfListView.XForms"
             xmlns:colours="clr-namespace:Nudge_.Shared"
             xmlns:syncCheckBox="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             xmlns:Defaults="clr-namespace:Nudge_.Shared"
             
             >
    
        <ContentPage.Resources>
        <ResourceDictionary>
            <Converters:FavouriteToImageConverter x:Key="boolToString"/>
            <Converters:SelectedItemEventArgsToSelectedItemConverter x:Key="SelectedItemConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                 <RowDefinition Height="0"/>
                 <RowDefinition Height="200"/>
                 <RowDefinition Height="1"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Image Grid.RowSpan="4" Source="{x:Static Defaults:DefaultVariables.backgroundImage}" Aspect="AspectFill"/>
            <Image Grid.RowSpan="4" BackgroundColor="{x:Static colours:ColourScheme.backgroundFilmColour}" Aspect="AspectFill"/>
            
            <StackLayout Grid.Row="1" Padding="10">
                <Label HorizontalOptions="Center" FontSize="Large" Text="Create Sliders" />
                <Entry x:Name="sliderTitle" Placeholder="Enter slider title"/>
                <StackLayout Orientation="Horizontal">
                    <StackLayout Orientation="Vertical" HorizontalOptions="StartAndExpand" >
                        <Entry x:Name="sliderNeg" Placeholder="Negative answer"/>
                    </StackLayout>
                    <StackLayout Orientation="Vertical" HorizontalOptions="EndAndExpand">
                        <Entry x:Name="sliderPos" Placeholder="Positive answer" HorizontalOptions="End" HorizontalTextAlignment="End"/>
                    </StackLayout>
                </StackLayout>
                <Button Text="  Add  " Command="{Binding AddNewSlider}"
                        BackgroundColor="{x:Static colours:ColourScheme.primaryButtonColour}"
                        TextColor="{x:Static colours:ColourScheme.primaryButtonTextColour}"
                        BorderColor="{x:Static colours:ColourScheme.primaryButtonBoarderColour}"
                        HorizontalOptions="End"/>
                <Label TextColor="{x:Static colours:ColourScheme.infoTextLightColour}" IsVisible="False" x:Name="sliderAddedLabel" HorizontalOptions="Center" Text="New Slider has been addded"/>
            </StackLayout>
            <BoxView Grid.Row="2" HeightRequest="1" BackgroundColor="Gray"     />       
                <syncfusion:SfListView
                      x:Name="SlidersListView"
                      SelectionMode="Single"
                      AutoFitMode="Height"
                      AllowSwiping="true"
                      SwipeOffset="80"
                      SwipeThreshold="30"
                    Grid.Row="3"
                      >
         <!--       <ListView.Behaviors>
                    <behaviours:EventToCommandBehavior
                        Source="{x:Reference SlidersListView}"
                        EventName="ItemSelected"
                        Command="{Binding SliderTapped}"
                        Converter="{StaticResource SelectedItemConverter}" />
                </ListView.Behaviors> -->
                
                    <syncfusion:SfListView.RightSwipeTemplate>
                      <DataTemplate x:Name="RightSwipeTemplate">
                        <Grid BackgroundColor="{Binding Source={x:Static colours:ColourScheme.editButtonColour }}" HorizontalOptions="Fill" VerticalOptions="Fill">
                            <Label Grid.Row="0"
                                    HeightRequest="50"
                                    WidthRequest="50" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"
                                    BackgroundColor="Transparent"
                                    Text="Edit" />
                            <Grid.GestureRecognizers>
                                   <TapGestureRecognizer Command="{Binding Path=BindingContext.EditSliderPressed, Source={x:Reference SlidersListView} }" CommandParameter="{Binding .}" />
                           </Grid.GestureRecognizers>
                        </Grid>
                      </DataTemplate>
                    </syncfusion:SfListView.RightSwipeTemplate>
                
                    <syncfusion:SfListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="1"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="40" />
                                    </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <StackLayout Padding="20">
                                        <StackLayout>
                                            <Label FontSize="Medium" x:Name="SliderTitle" HorizontalOptions="CenterAndExpand" Text="{Binding Title}"/>
                                        </StackLayout>
                                        <StackLayout Orientation="Horizontal">
                                            <StackLayout Orientation="Vertical" HorizontalOptions="StartAndExpand" >
                                                <Label x:Name="NegativeAnswer"  Text="{Binding NegativeAnswer}"/>
                                            </StackLayout>
                                            <StackLayout Orientation="Vertical" HorizontalOptions="EndAndExpand">
                                                <Label x:Name="PositiveAnswer" Text="{Binding PositiveAnswer}"/>
                                            </StackLayout>
                                        </StackLayout >
                                    </StackLayout>
                                 </Grid>
                                <Grid Grid.Column="1" Padding="5">
                                    <syncCheckBox:SfCheckBox UncheckedColor="#FFFFFF" CheckedColor="#FFFFFF" IsChecked="{Binding InUse}" StateChanged="Handle_StateChanged" CornerRadius="0" />
                                </Grid>
                                <Frame Grid.Row="1" Grid.ColumnSpan="2" OutlineColor="#E4E4E4" HasShadow="True" HeightRequest="1"/> 
                            </Grid>
                            </ViewCell>
                        </DataTemplate>
                   </syncfusion:SfListView.ItemTemplate>
                </syncfusion:SfListView>
            
        </Grid>
        
  </ContentPage.Content>
</ContentPage>